export const metadata = {
	title: 'Login – GoalStackr',
	description: 'A free goal tracking application',
};

export default function Layout({ children }: { children: React.ReactNode }) {
	return <div>{children}</div>;
}
